%h1 Categories
.row-fluid
  .span2
    = link_to "New Category", new_dashboard_category_path, class: 'btn btn-success'
  .span10
    %table.table
      %thead
        %tr
          %th Name
          %th
      %tbody
        - @categories.each do |category|
          %tr
            %td= category.name
            %td= link_to "edit", edit_dashboard_category_path(category)
